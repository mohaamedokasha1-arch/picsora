# Site identity — MUST be the exact production origin. Every URL in
# sitemap.xml, robots.txt, canonical tags, hreflang and JSON-LD is built from
# this value, so it has to match the real site domain 1:1.
# NOTE: only origins listed in VERIFIED_SITE_ORIGINS (lib/site.ts) are honoured;
# any other value is ignored with a warning and the verified canonical domain
# is used instead. To migrate to a custom domain: connect it, verify its DNS
# resolves, add it to VERIFIED_SITE_ORIGINS, then update this variable.
NEXT_PUBLIC_SITE_URL=https://piclizer.vercel.app
NEXT_PUBLIC_SITE_NAME=Piclizer

# SEO / Search Console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Analytics (disabled by default — loads only after user consent)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Advertising (disabled by default — loads only after user consent)
NEXT_PUBLIC_ADS_ENABLED=false
NEXT_PUBLIC_ADS_CLIENT_ID=

# Monetag — Vignette Banner. DISABLED by default: vignette/interstitial
# third-party ads conflict with Google AdSense programme policies while the
# site is under AdSense review, and the script also requires the visitor's
# advertising-cookie consent before it may load at all. Only set this to
# "true" if you deliberately accept that policy conflict.
NEXT_PUBLIC_MONETAG_ENABLED=false
# Zone id from your Monetag dashboard ("Get tag" for the Vignette Banner
# zone). Ignored unless NEXT_PUBLIC_MONETAG_ENABLED=true above.
NEXT_PUBLIC_MONETAG_VIGNETTE_ZONE_ID=11719435

# Contact form endpoint (optional; form is inert until this is set).
# Publicly visible endpoint URL that accepts POST {name, email, message}.
NEXT_PUBLIC_CONTACT_ENDPOINT=

# Bug-report form endpoint (optional). When unset (the default), the
# "Facing a problem or found a bug?" form on the contact page still works: it
# opens the visitor's email app with the report prefilled and addressed to
# piclizer@gmail.com (see reportEmail in lib/site.ts).
# Publicly visible endpoint URL that accepts POST {name, email, message} and
# delivers the report to piclizer@gmail.com (e.g. a Formspree/Web3Forms form).
# NOTE: if you set this, also add the endpoint's origin to connect-src in
# lib/security/headers.mjs — otherwise the browser's CSP blocks the request.
NEXT_PUBLIC_REPORT_ENDPOINT=
